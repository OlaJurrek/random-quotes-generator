import styles from "./Header.module.css";

const Header = () => (
  <header className={styles.header}>
    <h1 className={styles.headline}>Random qoutes generator</h1>
  </header>
);

export default Header;
